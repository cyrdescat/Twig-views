{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}

    <div class="row">
        {% for product in products %}
                <div class="col-4">
                    <div class="card" style="width: 18rem;"><img src="https://piximus.net/media/23571/dogs-with-eyebrows-15.jpg" class="card-img-top" alt="...">
                        <div class="card-body">
                            <p class="card-text">{{ product }}</p>
                        </div>
                    </div>
                </div>
        {% endfor %}
    </div>

{% endblock %}